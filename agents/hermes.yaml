interface:
  display_name: "Agent Travel"
  short_description: "Hermes scheduled research notes with local acceptance / Hermes 定时研究里的本地可控提示"
  default_prompt: "Use $agent-travel as a progressive-disclosure skill for Hermes scheduled, task-end, or explicit research windows. Generate a small redacted query plan first, keep large references unloaded until needed, and store only advisory-only hints for the next relevant turn. / 把 $agent-travel 用作 Hermes 的渐进式技能，只在 scheduled、task-end 或显式研究窗口触发。先生成小范围脱敏 query plan，需要时再加载大参考文件，并且只为下一次相关回合保存 advisory-only 提示。"

policy:
  allow_implicit_invocation: false
